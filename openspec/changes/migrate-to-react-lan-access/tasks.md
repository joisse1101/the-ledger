## 1. Backend foundation (Streamlit keeps working throughout)

- [x] 1.1 Add `fastapi`, `uvicorn`, `qrcode` to `requirements.txt` and `httpx` to `requirements-dev.txt` (leave `streamlit`/`pandas` for now); create `server.py` exposing a FastAPI `app` with `GET /api/meta` returning `refreshed_at`. Verify `pip install -r requirements-dev.txt` succeeds and a `TestClient` test in `tests/test_server.py` gets a 200 with a `refreshed_at` field.
- [x] 1.2 Give `claude_db._connect()` `PRAGMA journal_mode=WAL` and a `busy_timeout`, and make `startup()` and its `atexit` hook also delete the `-wal`/`-shm` files. Verify by extending `tests/test_claude_db.py`: after `startup()`, no `ledger.db*` file remains, and a read succeeds while a second connection holds an open write transaction on a `refresh()`-sized insert.
- [x] 1.3 Wire the server lifespan: call `claude_db.startup()` in a worker thread, and run a 10-minute loop task that calls a process-wide-locked `refresh()` in a worker thread. Add `POST /api/refresh` using the same lock. Verify with tests: two concurrent refresh calls run one after the other (not overlapping), `POST /api/refresh` advances `refreshed_at`, and the timer loop function calls refresh again after its interval (interval injected as a tiny value).
- [x] 1.4 Implement `LiveSnapshot` (lock + 1 s TTL) that builds the live payload from `load_sessions()` plus `live_context()` per session (one session's failure becomes `context: null`, others unaffected) and returns each context as `{size, growth, history, label}` with `label` from `format_context()`. Verify tests: a fake loader is called once for many calls within the TTL and again after it expires, and one session raising leaves the other sessions populated (mirrors the existing `_context_cell` cases in `tests/test_views.py`).

## 2. Pure logic moved out of `views/`

- [x] 2.1 Create `overview_stats.py` by lifting the pure parts of `views/overview.py` (`_TIME_RANGES`, `_filter_transcripts_by_range`, `_project_totals_dataframe` top-7 + Other, `_hourly_activity_dataframe` with hour trimming, `_format_duration`, summary metrics with the project/session named for longest/shortest/most-expensive/cheapest) as plain-Python functions that return dicts/lists with no pandas or Streamlit. Copy the cases from `tests/test_overview.py` into `tests/test_overview_stats.py` and verify they pass, including Today vs. Yesterday by local calendar date and the "Other" fold with ten projects.
- [x] 2.2 Create `transcript_query.py` porting `_filter_transcripts_dataframe` and `_sort_transcripts_dataframe` (literal case-insensitive search over session ID/last message/first prompt, AND-combined project/version/branch filters, stable sort with missing values last in both directions), plus paging (`limit`/`offset`, `total`) and distinct filter-option lists. Move the relevant cases from `tests/test_views.py` to `tests/test_transcript_query.py` and verify they pass, adding: literal `a.b` doesn't match `axb`, and sorting Context in both directions leaves `None` rows last.

## 3. API endpoints

- [x] 3.1 Add `GET /api/live` (from `LiveSnapshot`) and `GET /api/transcripts` (query params per design, default page size 50, each item flagged `live` using the same snapshot's live IDs, dates as ISO strings, `--` formatting left to the client). Verify with `TestClient` tests over `isolated_db` fixtures: filters combine, `total` reflects matches not the page, a live session is flagged, and a bad `sort` value returns 422 instead of raising.
- [x] 3.2 Add `GET /api/overview?range=` returning summary figures and chart-ready arrays (project totals with the shared top-7 + Other split and a stable project order for coloring, hourly buckets) from `overview_stats`. Verify tests: an unknown range returns 422, an empty range returns an empty-state payload (not an error), and the response's project order is the same for every chart array.
- [x] 3.3 Add `GET /api/sessions/{id}` returning the recap (title, last message or first prompt, start/updated, message count, cost, context) and `SessionDetail` as JSON, or `readable: false` when `load_detail()` returns `None`. Resolve `cwd` server-side from the live registry, else the transcripts snapshot; unknown ID returns 404. Validate `id` against `^[A-Za-z0-9-]{1,64}$` at the route. Verify tests: `../../x` and `a/b` are rejected without touching the filesystem, a client-supplied `cwd` query parameter is ignored, and a known session returns turns whose sums reconcile (first-turn context + attributed growth = current context, as in `tests/test_claude_context.py`).
- [x] 3.4 Add `DELETE /api/sessions/{id}` (fresh registry read, not the snapshot, to decide liveness; 409 for a live session, 404 for unknown, otherwise `delete_transcript`), `GET /api/projects`, and `DELETE /api/projects?path=` (exact match against known projects, else 404; calls `delete_project` then `delete_project_transcripts`). Verify tests: live session delete returns 409 and the file remains, a confirmed delete removes the file and the row, and a project path not in the snapshot is rejected and nothing is deleted.

## 4. Network access and auth

- [ ] 4.1 Implement the auth middleware: a request is local only when the client is loopback (`127.0.0.1`, `::1`, `::ffff:127.0.0.1`) and none of `Forwarded`/`X-Forwarded-For`/`X-Real-IP` is present; non-local requests need the token via the `ledger_token` cookie, `Authorization: Bearer`, or `?token=`; compare with `hmac.compare_digest`; refused requests get a 401 body pointing at the printed URL. It applies to every path, static files included. Verify with `TestClient(app, client=("192.168.1.50", 5000))` tests: no token, wrong token, and forwarded-from-loopback all get 401 with no data in the body; cookie and Bearer succeed; plain loopback succeeds with no token; a `DELETE` without the token from a remote client is 401 and deletes nothing.
- [ ] 4.2 Implement the sign-in redirect: a valid `?token=` answers 303 to the same URL with only the `token` parameter removed and sets `ledger_token` (`HttpOnly`, `SameSite=Strict`, `Path=/`, one-year max-age); a wrong `?token=` neither redirects nor sets a cookie. Verify tests for the `Set-Cookie` attributes, that other query parameters are preserved in the redirect target, and that a follow-up request using only the cookie is accepted.
- [ ] 4.3 Implement token provisioning: `LEDGER_TOKEN` if set, else read `.ledger/token`, else create it with `secrets.token_urlsafe(32)`; add `.ledger/` to `.gitignore`. Verify tests using a tmp dir: two consecutive provisions return the same token, the env var overrides and creates no file, and `git check-ignore .ledger/token` reports it ignored.
- [ ] 4.4 Add the cross-site and rebinding guards: no CORS middleware; every non-GET request must carry `X-Requested-With: ledger` (else 403); for loopback clients `Host` must be `localhost`/`127.0.0.1`/`[::1]` (any port), else 403. Verify tests: `POST /api/refresh` without the header is 403 and with it is 200; a loopback request with `Host: evil.example` is 403; a response never contains an `Access-Control-Allow-Origin` header, including for an `OPTIONS` preflight with a foreign `Origin`.
- [ ] 4.5 Implement the CLI in `server.py`: `python server.py [--lan] [--host] [--port]` (env `LEDGER_HOST`/`LEDGER_PORT` defaults; default host `127.0.0.1`, `--lan` = `0.0.0.0`, default port 8501), uvicorn with `access_log=False`. Verify by running it: `python server.py` serves on 127.0.0.1:8501 only (`curl http://127.0.0.1:8501/api/meta` works), `--port 9000` serves on 9000, and the terminal shows no request lines containing `token`.
- [ ] 4.6 Implement the startup banner: local URL always; with `--lan`, discover IPv4 addresses (hostname resolution plus the UDP-connect trick; drop loopback and 169.254.x.x), print `http://<ip>:<port>/?token=<token>` for each, an ASCII QR for the first via `qrcode`, and the "plain HTTP, trusted networks only" note. Verify a unit test for the address filter (fake addresses in, loopback/link-local out) and a manual `python server.py --lan` run where the printed QR opens the dashboard on the phone.
- [ ] 4.7 Serve the built front end: mount `web/dist` assets, SPA fallback to `index.html` for non-`/api` paths, gzip middleware; when `web/dist` is missing, print the build command at startup and answer non-API requests with a plain-text explanation. Verify tests with a tmp `dist`: `/overview` returns `index.html`, `/api/nope` returns 404 JSON not `index.html`, and with no `dist` the app starts and `/` returns the "not built" text.

## 5. Front-end scaffold and app shell

- [ ] 5.1 Create `web/` with Vite + React + TypeScript, React Router, TanStack Query, Vitest; add a Vite dev proxy from `/api` to `http://127.0.0.1:8501`; add `web/node_modules/` and `web/dist/` to `.gitignore`. Verify `npm ci && npm run build` produces `web/dist/index.html`, `npm run dev` proxies `/api/meta` to the running server, and `npm test` runs.
- [ ] 5.2 Add a typed API client (`fetch` wrapper that adds `X-Requested-With: ledger` to non-GET calls, surfaces 401 as an "open the printed link" state and other failures as errors) and query hooks for each endpoint. Verify with Vitest using a mocked `fetch`: the header is sent on DELETE and not required on GET, a 409 delete surfaces the server's message, and a network failure keeps the previous query data.
- [ ] 5.3 Build the theme: CSS variables for the neutral tokens and the 8-slot categorical palette (light and dark sets copied from `_CATEGORICAL_LIGHT`/`_CATEGORICAL_DARK` in `views/overview.py`, plus `_MUTED_INK`), `data-theme` on `<html>` from `localStorage` (try/catch) else `prefers-color-scheme`, and a toggle. Verify in a browser: with no saved choice the theme follows the OS setting, the toggle persists across reload, and toggling in one browser profile doesn't change another.
- [ ] 5.4 Build the app shell: routes `/` (Sessions), `/overview`, `/projects`; a `useViewportClass()` hook (matchMedia at 640/1024) with a Vitest test for the three classes; a nav rendering as a top bar (medium/wide) or fixed bottom tab bar (narrow) with the refresh control ("last refreshed" from `/api/meta`, `POST /api/refresh`) and theme toggle; safe-area padding and `viewport-fit=cover`; a global "can't reach the server / data may be out of date" banner with retry. Verify in a browser: reload keeps the route, back/forward works, the bottom bar doesn't cover the last content at 390 px, refresh updates the timestamp, and stopping the server shows the banner and restarting it clears it.

## 6. Sessions page

- [ ] 6.1 Build the shared list component that renders the same rows as a `<table>` (medium hides low-priority columns; wide shows all, sortable headers) or as cards (narrow, key fields per the responsive spec), one renderer in the DOM at a time, with one click handler and one order. Verify a Vitest/React Testing Library test that both renderers emit the same row IDs in the same order and that clicking either calls the handler with that row.
- [ ] 6.2 Build the Live list: 2 s `refetchInterval`, `refetchIntervalInBackground: false`, refetch on visibility, Auto-refresh switch (default on; off sets the interval to false and freezes the "last refreshed" time), empty state text, and the Context cell showing `label` (or `--`). Verify in a browser with the network tab: requests fire about every 2 s, stop completely when the tab is backgrounded and fire once on return, and the switch stops them; start a Claude session and confirm its row appears and its context changes within a few seconds.
- [ ] 6.3 Build the All list: search box (debounced), Project/Version/Branch multi-selects fed by the API's option lists, sort (clickable headers on table layouts; a "Sort by" select plus direction button on cards), page size 50 with "Load more" and a total count, live badge, `--` for missing values, empty-filter message, inputs at 16 px font size. Verify in a browser: typing `a.b` matches literally, filters combine, sorting by Context puts `--` rows last both ways, and Load more appends without resetting scroll.
- [ ] 6.4 Build the detail view on a native `<dialog>` (`showModal()`): full-screen sheet with sticky header and always-visible close button under 640 px, centered dialog above; recap block (All list only); "Current context" figure; tokens-per-response chart; all-responses table (scrolls inside its own container); compaction captions; "what filled the context" by-tool table and five largest increases; `readable: false` message; open state kept in the URL or component state so a Live poll updates it in place. Verify in a browser: opening from Live and from All shows the right blocks, staying open across 6+ Live ticks without losing scroll position, closing returns the list to its scroll offset, and at 390 px the sheet fills the screen.
- [ ] 6.5 Port the detail chart: the stacked Cache read / Cache written / New bars with ▼ cache-miss markers and dashed compaction rules from `views/sessions_context.py` as a lazily imported `vega-embed` module (container width, colors from the theme variables, re-render on theme and container size change). Verify against the largest local transcript (`c2cf6150-07ea-4320-885a-cfb7ada6539b.jsonl` if still present): miss and compaction markers appear where the Streamlit dialog shows them, and the chart re-fits when the window is resized to 320 px.
- [ ] 6.6 Add the delete flow to the All-list detail: a Delete button, an inline confirmation ("cannot be undone") with Confirm/Cancel, disabled with the "still live" note when the session is live, list refetch on success, and the server's 409 message shown if a race makes it live. Verify: cancel deletes nothing, confirm removes the row (on a throwaway transcript copy, not real history), a live session shows no delete control, and `curl -X DELETE` against a live ID with the header returns 409.

## 7. Overview page

- [ ] 7.1 Build the time-range selector (seven ranges, default All time; wraps or scrolls inside itself on narrow screens) and the summary figures grid (two per row narrow, wider grids above), with the extreme-value figures as buttons that toggle an inline note naming the project and session ID (also shown on hover for pointer devices). Verify in a browser at 360 px: all seven ranges are reachable with no horizontal page scroll, tapping "Longest session" shows the project and ID, and figures match the Streamlit Overview for the same range.
- [ ] 7.2 Port the charts as `vega-embed` specs sharing the API's project order and the theme variables: sessions-by-project donut, Messages & Cost by Project and Activity by Hour grouped bars normalized to % of peak with their explanatory captions, "Other" in the muted color, "Messages" on categorical slot 0 in both bar charts. Verify side by side with Streamlit for the same range: same slices, same colors per project across charts, same hour range, captions present, and no horizontal scroll or clipped labels at 320 px.
- [ ] 7.3 Implement the width-class layouts: narrow single column, medium charts stacked with a wider figure grid, wide donut beside the figures with the bar charts full-width below; empty-range message. Verify in a browser at 360, 768, and 1280 px that each layout matches the spec, and that selecting Yesterday on a day with no sessions shows the "no sessions for yesterday" text.

## 8. Projects page

- [ ] 8.1 Build the Projects list (cards narrow, table medium/wide) with all fields (name, path, trust, last session, version, last cost, last start, lines +/-, MCP servers) and the delete flow: Delete → inline confirmation naming the path and saying it cannot be undone → Confirm/Cancel, then refetch of projects, transcripts, and overview data. Verify in a browser using a throwaway entry in a temporary `~/.claude.json` copy (via the `isolated_db`-style env override, not real data): confirmation shows the path, cancel deletes nothing, confirm removes the entry and its sessions from the All list, and the empty state text shows when none remain.

## 9. Responsive and phone verification

- [ ] 9.1 Audit every page at 320, 390, 768, 1024, and 1280 px in Chrome (resize the window; also rotate via device emulation): no horizontal page scrollbar (`document.documentElement.scrollWidth <= innerWidth` checked from the console), cards below 640 and tables above, bottom nav only below 640, all interactive elements at least 44 px tall on the narrow layout (checked with a small console script over `button, a, select, input`), and no state loss (page, search text, filters, open detail) when the width crosses each breakpoint. Fix anything that fails and note the result.
- [ ] 9.2 Verify on a real phone over Wi-Fi: run `python server.py --lan`, accept the Windows Firewall prompt for Private networks, scan the QR, and confirm the dashboard loads, the address bar no longer shows the token, revisiting the plain address works, the Live list updates while a Claude session runs, the detail sheet fills the screen, focusing the search field doesn't zoom, and Overview charts fit. Then confirm a second unauthenticated device (or an incognito phone tab on the plain address) gets the 401 message and no data.

## 10. Cutover and cleanup (only after 5–9 pass)

- [ ] 10.1 Compare each page against the still-running Streamlit app on the same data and list any gaps against the `web-dashboard` spec; fix gaps before continuing. Verify by walking every requirement's scenarios in `specs/web-dashboard/spec.md` and ticking each off in a short note appended to this task.
- [ ] 10.2 Move `claude_db.db_path()` to `.ledger/ledger.db` and update the docstring; update `.gitignore` (drop the `.streamlit/*` lines, keep `.ledger/`, `web/node_modules/`, `web/dist/`). Verify `pytest` passes and that after starting and stopping the server no `ledger.db*` remains in `.ledger/` while `.ledger/token` does.
- [ ] 10.3 Delete `app.py`, `views/`, `.streamlit/config.toml`, `tests/test_app.py`, `tests/test_views.py`, and `tests/test_overview.py` (their cases now live in the new test files), and remove `streamlit` from `requirements.txt` (and `pandas` if `grep -r "import pandas"` finds nothing outside `.venv`). Verify `pip install -r requirements-dev.txt` in a fresh venv, `pytest` passes, and `python server.py` serves the built app with no Streamlit installed.
- [ ] 10.4 Rewrite the `CLAUDE.md` Project/Setup/Architecture sections (FastAPI + `web/`, API endpoints, auth model, server-side refresh, `.ledger/`, responsive strategy, `npm run build`, how to run tests for both sides) and update `README.md` (setup incl. Node build, `python server.py` and `--lan`, phone sign-in via QR, Windows Firewall note, token rotation, plain-HTTP warning, per-device theme replacing the sidebar dark mode note). Verify by following the README from a clean clone to a working local run and a phone connection.
- [ ] 10.5 Run `openspec validate migrate-to-react-lan-access --strict`, the full `pytest` suite, `npm test`, and `npm run build`. Verify all exit 0 and note in this task the test counts before and after.
